package TicTacToe;

import java.util.List;

public class GreedyPlayer implements Player{
    
    public Move generateMove(Board b) {
        // first checks if it can win on the next move, then if it can block an opponent win, else plays a random move
        return null;
    }
}
